extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/product-layout.pug

block main 
    .container.my-3 
        .row 
            .col-12 
               +box-head("Result")
        if(products.length > 0)
            +product-grid(products)
        else 
            h3(class="text-center") Cannot find any results
            