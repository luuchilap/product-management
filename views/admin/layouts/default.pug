doctype html
html 
    head 
        title #{pageTitle}
        //- dau / the hien dang o folder public
        link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css")
        link(rel="stylesheet", href="/admin/css/style.css")
    body 
        if (role.permissions.length > 0)
            include ../partials/header.pug
            .body 
                include ../partials/sider.pug

                .main 
                    block main
            div #{role.permissions}
            script(src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.slim.min.js")
            script(src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js")
            //- ghi / thì tự hiểu thành đang ở folder public
            script(src="/tinymce/tinymce.min.js") 
            script(src="/admin/js/tinymce-config.js")
            
            script(src="/admin/js/script.js") 
        else 
            h1 You are not allowed to access this page.
